# Рекомендации по микроразметке для ПРОМІМПЕКС СЄВЄР

## ✅ Что уже реализовано

### 1. **Organization Schema** 
- Полная информация о компании
- Контактные данные
- Логотип и изображения
- **Где:** `lib/seo.ts` → `generateStructuredData()`

### 2. **LocalBusiness Schema** (улучшенная)
- Адрес и геолокация
- Часы работы
- Телефоны и email
- Зона обслуживания (Украина)
- **Где:** `lib/seo.ts` → `generateStructuredData()`

### 3. **WebSite Schema с SearchAction**
- Информация о сайте
- Поисковая функция
- **Где:** `lib/seo.ts` → `generateStructuredData()`

### 4. **BreadcrumbList Schema**
- Навигационная цепочка
- **Где:** `lib/seo.ts` → `generateStructuredData()`

### 5. **ItemList для продуктов**
- Список всех 8 продуктов
- Каждый продукт как Product schema
- **Где:** `lib/seo.ts` → `generateStructuredData()`

### 6. **Service Schema** (4 основных услуги)
- Ремонт РВД
- Производство РВД
- Ремонт гидроцилиндров
- Поставка гидроарматуры
- **Где:** `lib/seo.ts` → `generateStructuredData()`

### 7. **AggregateRating и Review Schema**
- Средний рейтинг (5.0)
- 3 отзыва клиентов
- **Где:** `lib/seo.ts` → `generateStructuredData()`

## 📍 Где размещена микроразметка

Все схемы автоматически добавляются в `<head>` каждой страницы через компонент:
- `components/SEO/StructuredData.tsx`
- Подключается в `app/layout.tsx`

## 🚀 Дополнительные рекомендации

### 1. **Добавить координаты (GeoCoordinates)**
Если у вас есть GPS координаты офиса, добавьте в `lib/seo.ts`:
```typescript
geo: {
  '@type': 'GeoCoordinates',
  latitude: '49.4229', // пример
  longitude: '26.9871', // пример
}
```

### 2. **Добавить FAQPage Schema** (если появятся FAQ)
Создайте компонент FAQ и добавьте схему:
```typescript
{
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: [
    {
      '@type': 'Question',
      name: 'Сколько стоит ремонт РВД?',
      acceptedAnswer: {
        '@type': 'Answer',
        text: 'Цена зависит от...'
      }
    }
  ]
}
```

### 3. **Добавить VideoObject Schema** (если будут видео)
Для видео о продукции или услугах:
```typescript
{
  '@type': 'VideoObject',
  name: 'Название видео',
  description: 'Описание',
  thumbnailUrl: 'URL превью',
  uploadDate: '2024-01-01',
  contentUrl: 'URL видео'
}
```

### 4. **Добавить HowTo Schema** (для инструкций)
Если будут инструкции по использованию:
```typescript
{
  '@type': 'HowTo',
  name: 'Как заменить РВД',
  step: [...]
}
```

### 5. **Улучшить Product Schema для отдельных продуктов**
В модальном окне продукта (`ProductModal.tsx`) можно добавить динамическую микроразметку:
```typescript
// В ProductModal.tsx добавить useEffect:
useEffect(() => {
  if (product && isOpen) {
    const productSchema = {
      '@context': 'https://schema.org',
      '@type': 'Product',
      name: product.title,
      description: product.description,
      // ... остальные поля
    };
    // Добавить script в head
  }
}, [product, isOpen]);
```

### 6. **Добавить Offer Schema для продуктов**
Если указываете цены:
```typescript
{
  '@type': 'Offer',
  price: '1000',
  priceCurrency: 'UAH',
  availability: 'https://schema.org/InStock',
  url: `${siteConfig.url}/#products`
}
```

### 7. **Добавить Social Media Links**
В `organizationSchema` добавьте реальные ссылки:
```typescript
sameAs: [
  'https://www.facebook.com/yourpage',
  'https://www.instagram.com/yourpage',
  // и т.д.
]
```

## 🔍 Проверка микроразметки

### Инструменты для проверки:
1. **Google Rich Results Test**: https://search.google.com/test/rich-results
2. **Schema.org Validator**: https://validator.schema.org/
3. **Google Search Console**: Проверка в разделе "Улучшения"

### Что проверить:
- ✅ Все схемы валидны
- ✅ Нет ошибок в консоли
- ✅ Google видит все данные
- ✅ Rich snippets отображаются в поиске

## 📊 Ожидаемые результаты

После добавления микроразметки Google сможет:
1. **Показывать расширенные сниппеты** в поиске:
   - Звездочки с рейтингом
   - Часы работы
   - Телефоны (кликабельные)
   - Адрес с картой

2. **Лучше понимать контент**:
   - Тип бизнеса
   - Услуги и продукты
   - Географию обслуживания

3. **Улучшить релевантность**:
   - Более точное соответствие запросам
   - Лучшие позиции в локальном поиске
   - Появление в Google Maps

## 🎯 Приоритетные улучшения

1. ⭐ **Добавить координаты** (GeoCoordinates) - для Google Maps
2. ⭐ **Добавить реальные ссылки на соцсети** - для доверия
3. ⭐ **Создать FAQ страницу** - для FAQPage schema
4. ⭐ **Добавить цены на продукты** - для Offer schema

## 📝 Примечания

- Все схемы генерируются автоматически при загрузке страницы
- Микроразметка обновляется при изменении данных в `lib/seo.ts`
- Для добавления новых схем используйте функцию `generateStructuredData()`

